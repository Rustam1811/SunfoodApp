rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY: Open rules for development/testing
    // TODO: Restrict before production launch
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
